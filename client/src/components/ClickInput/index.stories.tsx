import { storiesOf } from "@storybook/react";
import React from "react";
import Input from ".";
import { useInput } from "hooks/useInput";
storiesOf("Components/Inputs/Input", module).add("with label sm", () => <> </>);
